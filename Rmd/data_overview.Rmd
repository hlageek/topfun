---
title: "Overview of the data"
output: 
  html_document:
      toc: true
      code_folding: hide
params:
    targets_store: "../_targets"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(targets)
library(tidyverse, quietly = TRUE)
```

```{r setup_store, eval=FALSE, include=FALSE}
# for interactive work only
params$targets_store <- "_targets"
```

## Combined dataset

```{r combined_dataset}
tar_load(data_all, store = params$targets_store)
```

Number of observations: `r nrow(data_all)`

Variables: `r colnames(data_all)`

Preview: 
```{r}
head(data_all) %>% 
    kableExtra::kable(escape = F) %>% 
    kableExtra::text_spec(3, extra_css = 'white-space: nowrap; overflow: hidden; text-overflow: ellipsis;'
                          )
```

